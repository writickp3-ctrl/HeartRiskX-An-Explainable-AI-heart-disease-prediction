# Versions pinned to combinations confirmed working (Windows, Python 3.14).
# If installing fresh elsewhere, these are safe floors, not hard requirements.
streamlit>=1.60
pandas>=2.0
numpy>=1.26
scikit-learn==1.9.0
lightgbm==4.7.0
shap>=0.50
joblib>=1.3
matplotlib>=3.8
